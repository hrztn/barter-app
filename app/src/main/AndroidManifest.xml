<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barterapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BarterApp">
        <activity android:name=".CustomRequestActivity" />
        <activity android:name=".TradeDetailsActivity" />
        <activity android:name=".MyTradeActivity" />
        <activity android:name=".PublicUserViewActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".RequestTradeActivity" />
        <activity android:name=".ItemInfoActivity" />
        <activity android:name=".NewItemActivity" />
        <activity android:name=".EditItemActivity" />
        <activity android:name=".SearchItemActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>